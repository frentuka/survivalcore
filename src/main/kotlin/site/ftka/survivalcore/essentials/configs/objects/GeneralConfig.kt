package site.ftka.survivalcore.essentials.configs.objects

import site.ftka.survivalcore.essentials.configs.interfaces.ConfigFile

class GeneralConfig: ConfigFile {

    val PLAYER_FIRST_JOIN_SPAWN_LOCATION = Triple(0.5, 100.0, 0.5)
}